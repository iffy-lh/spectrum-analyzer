# 基于STM32F103的实时音频频谱分析与噪音识别系统

## 技术报告

---

> **项目名称**：频谱分析仪 / 噪音识别分析系统  
> **作者**：iffy-lh  
> **GitHub**：https://github.com/iffy-lh/spectrum-analyzer  
> **日期**：2025年6月  

---

## 摘要

本文介绍了一套基于 STM32F103ZET6 微控制器的实时音频频谱分析与噪音识别系统。系统分为两部分：嵌入式端采用正点原子 F1 精英版开发板，通过 MAX9814 麦克风模块采集环境音频，利用 CMSIS-DSP 库进行 256 点 FFT 实时频谱分析，结果通过 USB CDC 虚拟串口发送至 PC；PC 端基于 Python 实现实时频谱可视化、噪音分类（规则匹配 + ML 骨架）和烦恼度综合评估。系统可应用于环境噪音监测、声学实验教学等场景。

**关键词**：STM32F103；FFT；频谱分析；CMSIS-DSP；噪音分类；嵌入式系统

---

## 1. 系统概述

### 1.1 项目背景

传统音频频谱分析依赖昂贵的专用仪器或高性能计算平台。本系统利用廉价 STM32 开发板配合 PC 端 Python 分析引擎，实现了一套低成本、可扩展的实时频谱分析与噪音识别方案。

### 1.2 系统架构

```
┌─────────────────────────────────────────────────┐
│                  STM32 嵌入式端                    │
│                                                   │
│  MAX9814 → PA1(ADC1) → DMA → FFT(CMSIS-DSP)      │
│                ↓                 ↓                │
│            TFT LCD            USB CDC             │
│          (实时频谱柱状图)    (虚拟串口)             │
└─────────────────────────────────────────────────┘
                         │ USB CDC (COMx, 115200bps)
                         ↓
┌─────────────────────────────────────────────────┐
│                PC 端 Python 分析系统               │
│                                                   │
│  串口接收 → 频谱可视化 → 噪音分类 → 烦恼度评估     │
│                                                   │
│  · 终端 ASCII 频谱 (terminal_analyzer.py)         │
│  · matplotlib 实时图 (live_analyzer.py)           │
│  · 噪音分类器 (noise_classifier.py)               │
│  · 烦恼度评估器 (annoyance_evaluator.py)          │
└─────────────────────────────────────────────────┘
```

---

## 2. 硬件设计

### 2.1 核心器件

| 组件 | 型号 | 说明 |
|------|------|------|
| 主控芯片 | STM32F103ZET6 | Cortex-M3, 72MHz, 64KB RAM, 512KB Flash |
| 开发板 | 正点原子 F1 精英版 | 集成 TFT LCD、ST-Link、USB 接口 |
| 麦克风模块 | MAX9814 | 驻极体麦克风 + AGC 自动增益放大器 |
| 显示屏 | 3.2寸 TFT LCD (ILI9341) | 320×240 分辨率，16位色 |

### 2.2 引脚连接

| MAX9814 引脚 | 连接 | 说明 |
|-------------|------|------|
| VCC | 3.3V | 电源 |
| GND | GND | 地 |
| OUT | PA1 (ADC1_CH1) | 模拟音频输出 |
| GAIN | VDD (3.3V) | 增益 50dB（语音模式推荐） |
| AR | 悬空 | 默认 AGC 模式 |

### 2.3 通信接口

- **USB_SLAVE 口**：USB CDC 虚拟串口，用于将 FFT 频谱数据发送至 PC
- **USB_232 口** (CH340G)：ST-Link 调试/烧录接口
- 波特率：115200 bps (USB CDC)

---

## 3. STM32 固件设计

### 3.1 软件工具链

- IDE：Keil MDK5
- 编译器：ARM Compiler 6 (AC6)
- 烧录：ST-Link SWD
- DSP 库：CMSIS-DSP (arm_cortexM3l_math.lib)
- 宏定义：`ARM_MATH_CM3`

### 3.2 项目结构

```
firmware/
├── User/main.c              # 主程序：ADC采集 + FFT + LCD显示 + 串口发送
├── User/archive/            # 历史版本备份
├── Drivers/
│   ├── BSP/ADC/adc.c        # ADC 驱动（含硬件复位 + 校准）
│   ├── BSP/DMA/dma.c        # DMA 驱动
│   ├── BSP/LCD/lcd.c        # TFT LCD 驱动
│   ├── CMSIS/DSP/Include/   # CMSIS-DSP 头文件
│   ├── CMSIS/Lib/ARM/       # 预编译 DSP 库
│   └── SYSTEM/              # 正点原子系统工具 (delay, sys, usart)
├── Middlewares/USB/          # USB CDC 虚拟串口协议栈
└── Projects/MDK-ARM/        # Keil 工程文件
```

### 3.3 ADC + DMA 采集设计

系统使用 ADC1 通道 1 (PA1) 以连续转换模式采集音频信号。

**关键配置：**

- ADC 时钟：12 MHz (PCLK2/6)
- 采样时间：239.5 周期
- 采样率：~47.6 kHz
- DMA 模式：非循环模式 (CIRC=0)，每次传输完成后由 ISR 重启
- 缓冲区大小：256 采样点（对应 256 点 FFT）

**初始化序列（已验证可正常工作）：**

```
1. ADC 硬件复位 (RCC->APB2RSTR)
2. CR2 = DMA=1, CONT=0, ADON=0
3. ADON=1 → 校准 (CONT=0 保证 ADC 未在转换)
4. ADON=0 → CONT=1
5. DMA 配置
6. ADON=1 → 启动连续转换
```

初始化完成后 `ADC1->CR2` 应读数为 `0x0103` (ADON=1, CONT=1, DMA=1)。

### 3.4 FFT 频谱分析

**配置参数：**

| 参数 | 值 |
|------|---|
| FFT 点数 | 256 |
| 频率分辨率 | 186 Hz/bin |
| 有效频率范围 | 186 Hz ~ 23.8 kHz |
| 窗函数 | 汉宁窗 (Hanning) |
| FFT 函数 | arm_cfft_f32 (CMSIS-DSP, radix-4) |

**信号处理流程：**

1. 256 点 ADC 原始数据 → 减去 DC 偏置 (2048) → 乘以汉宁窗
2. CMSIS-DSP `arm_cfft_f32` 复数 FFT
3. `arm_cmplx_mag_f32` 计算幅度谱
4. 汉宁窗幅度补偿 (×2)
5. 取前 128 个 bin 发送至 PC

### 3.5 LCD 显示

- 对数频率轴：平滑显示低频细节
- 128 根频谱柱，亮蓝色 (0x07FF)
- 快升慢降包络：上升瞬时响应，下降指数衰减 (FALL_RATE 宏可调)
- 显示模式宏 `DEMO_MODE`：切测试信号 / 真实 ADC

### 3.6 串口通信协议

系统采用自定义二进制帧协议，用于 STM32 向 PC 传输 FFT 频谱数据：

```
┌──────┬──────┬──────┬──────┬──────────────────────┬──────┐
│ 0xAA │ 0x55 │ lenL │ lenH │  N × uint16 (小端)   │ XOR  │
│ 同步头(2B)  │ 长度(2B)   │   FFT 幅度数据(N×2B)   │ 校验 │
└──────┴──────┴──────┴──────┴──────────────────────┴──────┘
```

**协议特点：**
- 同步头 0xAA 0x55：接收端可从此恢复字节对齐
- XOR 校验：覆盖数据区所有字节，简单可靠
- 每帧发送 128 个 uint16 FFT 幅度值
- STM32 端通过 `USART1->DR` 直接寄存器写入，无需 printf

---

## 4. PC 端 Python 分析系统

### 4.1 环境要求

```bash
# 依赖安装
pip install numpy pyserial
# 可选（matplotlib 图表版）
pip install matplotlib scikit-learn
```

### 4.2 模块架构

```
pc_analyzer/
├── main.py                  # 主入口 (WAV/串口/Demo 三种模式)
├── terminal_analyzer.py     # 终端 ASCII 频谱 (无需 matplotlib)
├── live_analyzer.py         # matplotlib 实时频谱图 + 噪音分类
├── live_noise.py            # 轻量终端版 (ASCII 频谱 + 中文噪音分类)
├── serial_receiver.py       # 串口数据接收模块
├── audio_features.py        # 音频特征提取 (FFT/MFCC/谐波检测)
├── noise_classifier.py      # 噪音分类器 (规则 + ML)
├── annoyance_evaluator.py   # 烦恼度/危害评估器
├── sound_library.py         # 噪音音库查询
├── sound_library.json       # 噪音类型配置文件
└── requirements.txt         # Python 依赖清单
```

### 4.3 噪音分类器

采用两层分类策略：

**Layer 1 — 规则匹配（开箱即用）：**

基于频段能量比 + 频谱特征的快速初筛，10 条规则按优先级排序：

| 优先级 | 类型 | 检测特征 |
|--------|------|----------|
| 110 | 安静 | 总能量 < 阈值 |
| 100 | 啸叫 | 高频能量 > 40% |
| 85 | 撞击 | 宽频带短时高能量 |
| 80 | 工频噪声 | 50Hz 倍频特征 |
| 70 | 说话声 | 中频 (558-4100Hz) 能量 > 25%，谐波结构 |
| 65 | 施工 | 全频带高能量 |
| 55 | 交通 | 低频 > 45%，中频 < 25% |
| 45 | 空调/风扇 | 低频 > 65%，中频 < 20% |
| 35 | 人群嘈杂 | 中频 > 30% |
| 25 | 自然声 | 分布均匀 |

**Layer 2 — ML 分类器（骨架，需训练数据）：**

RandomForest 分类器，输入特征向量包括 6 频段能量比、谱质心、谱带宽、MFCC 前 13 系数、谐波数量等。

### 4.4 烦恼度评估器

综合 6 维评分模型：

```
烦恼度总分 = SPL_score × 类型权重 × 时间调整 × 时长效应 × 人声惩罚 × 环境系数
```

| 维度 | 说明 |
|------|------|
| A 计权声压级 | 模拟人耳对不同频率的敏感度差异 |
| 噪音类型权重 | 啸叫(0.95) > 施工(0.90) > 交通(0.75) > 说话声(0.60*) |
| 时间情境 | 夜间 (22:00-06:00) 加倍 |
| 暴露时长 | 持续暴露累积伤害效应 |
| 人声特殊处理 | 带语义信息的噪音更恼人 |

---

## 5. 部署指南

### 5.1 固件烧录 (STM32)

1. 用 Keil MDK 打开 `firmware/Projects/MDK-ARM/atk_f103.uvprojx`
2. 编译器选择 ARM Compiler 6
3. F7 编译 → F8 下载 (ST-Link SWD)
4. 板子 USB_SLAVE 口连接 PC

### 5.2 PC 端安装运行

**前置条件：**
- Windows 10/11
- Python 3.8+
- STM32 板已烧录固件并通过 USB_SLAVE 连接

**步骤：**

```powershell
# 1. 安装 Python 依赖
pip install pyserial numpy

# 2. 克隆或复制 pc_analyzer/ 文件夹到本地

# 3. 查找 COM 口号
powershell -Command "[System.IO.Ports.SerialPort]::GetPortNames()"

# 4. 运行（终端版，推荐）
cd pc_analyzer
python terminal_analyzer.py COM12

# 或运行（轻量版，带中文分类）
python live_noise.py COM12
```

### 5.3 在其他电脑上部署

只需三步：

1. **装 Python + 依赖**：`pip install pyserial numpy`
2. **拷代码**：把 `pc_analyzer/` 文件夹复制过去
3. **插板子跑**：`python live_noise.py COM口号`

STM32 固件已烧录在板子上，换电脑无需重新烧录。USB CDC 驱动 Windows 10/11 自动识别。

### 5.4 故障排查

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| COM 口不存在 | 板子未连接或驱动未安装 | 重新插拔 USB_SLAVE 线，检查设备管理器 |
| COM 口列出但打不开 | USB CDC 状态异常 | 重新插拔 USB 线 |
| 无数据显示 | STM32 固件未运行 | 检查板子电源灯，确认固件已烧录 |
| 频谱无变化 | 麦克风未接或增益不够 | 检查 MAX9814 接线，GAIN 接 VDD |
| 噪音分类不准 | 环境噪音与规则不匹配 | 对着麦克风说话/拍手测试 |

---

## 6. 版本历史

| 版本 | 日期 | 内容 |
|------|------|------|
| v1-v3 | 2025 | 基础框架：TFTLCD 显示、DMA+ADC、CMSIS-DSP FFT |
| v4 | 2025-06 | MAX9814 真实 ADC 采集、汉宁窗 + 幅度补偿 |
| v5 | 2025-06 | ADC+DMA 初始化修复（硬件复位 + 校准顺序） |
| v6 | 2025-06 | USB CDC 虚拟串口、二进制帧协议、PC 端 ASCII 频谱 |
| v7 | 2025-06 | PC 端完整分析系统：噪音分类 + 烦恼度评估 + 多模式 |

---

## 参考文献

[1] STMicroelectronics. RM0008 Reference Manual: STM32F101xx, STM32F102xx, STM32F103xx advanced ARM-based 32-bit MCUs. 2021.

[2] ARM Limited. CMSIS-DSP Software Library. https://github.com/ARM-software/CMSIS-DSP

[3] 正点原子. STM32F1 开发指南（寄存器版）. 广州星翼电子科技有限公司.

[4] Maxim Integrated. MAX9814 Microphone Amplifier with AGC and Low-Noise Microphone Bias. Datasheet, 2012.

[5] Smith, S.W. The Scientist and Engineer's Guide to Digital Signal Processing. California Technical Publishing, 1997.

[6] Oppenheim, A.V. & Schafer, R.W. Discrete-Time Signal Processing. 3rd Edition, Pearson, 2010.

[7] Zwicker, E. & Fastl, H. Psychoacoustics: Facts and Models. 3rd Edition, Springer, 2007.

[8] ISO 226:2003. Acoustics — Normal equal-loudness-level contours. International Organization for Standardization, 2003.

---

> 完整源代码：https://github.com/iffy-lh/spectrum-analyzer
